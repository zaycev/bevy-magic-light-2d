name: PR

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v3
    - name: Build
      run: echo "works"

    - name: Trunk Check
      uses: trunk-io/trunk-action@v1
      with:
        cache: true